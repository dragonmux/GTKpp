# GTK++ pkg-config file

prefix=@PREFIX@
exec_prefix=${prefix}
libdir=@LIBDIR@
includedir=${prefix}/include/GTK++

Name: libGTK++
Description: libGTK++ is a general purpose C++ binding for GTK+ that incorperates the gtkGLExt extension for GTK+ to allow for applications to do graphics from within OpenGL
Version: 1.0.0
Requires:
Conflicts:
Libs: -L${libdir} -lGTK++
Cflags: -I${includedir} @PACKAGES@
